import { DirPessoalDirective } from './dir-pessoal.directive';

describe('DirPessoalDirective', () => {
  it('should create an instance', () => {
    const directive = new DirPessoalDirective();
    expect(directive).toBeTruthy();
  });
});
