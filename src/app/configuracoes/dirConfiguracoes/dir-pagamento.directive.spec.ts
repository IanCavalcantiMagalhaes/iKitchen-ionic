import { DirPagamentoDirective } from './dir-pagamento.directive';

describe('DirPagamentoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirPagamentoDirective();
    expect(directive).toBeTruthy();
  });
});
