import { DirEntregaDirective } from './dir-entrega.directive';

describe('DirEntregaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirEntregaDirective();
    expect(directive).toBeTruthy();
  });
});
