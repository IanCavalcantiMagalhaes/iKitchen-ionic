import { DirCartaoDirective } from './dir-cartao.directive';

describe('DirCartaoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirCartaoDirective();
    expect(directive).toBeTruthy();
  });
});
