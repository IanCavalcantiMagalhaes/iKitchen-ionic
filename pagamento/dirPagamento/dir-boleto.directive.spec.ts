import { DirBoletoDirective } from './dir-boleto.directive';

describe('DirBoletoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirBoletoDirective();
    expect(directive).toBeTruthy();
  });
});
